# A single page app using React

This project is created during my React-learning process. It contains multiple features including reading and manipulating data from a remote database(supabase in this case), front-end UI design and user authentication.
